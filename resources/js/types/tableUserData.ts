export type UserTypes = {
    id: number;
    name: string;
    role: number;
    email: string;
    avatar: string;
    team_id: number | null;

    // savings: number; //貯金の合計金額
    // investment: number; //投資の合計金額
    // need: number; //必要
    // want: number; //欲しい結果
    // donation: number; //寄付の合計金額
    // game_level: number; //欲しい予定
    // game_life: number;
};
